# 静默启动管理器 V21 · 交付说明

**构建时间**：2026-08-15 22:40（本版新增「首次启动权限引导」）
**交付物**：
- `12-silent-launcher-universal.dmg`（universal arm64+x86_64，≈3.5MB）
- `10.15-silent-launcher-x86_64.dmg`（x86_64，LSMin 10.15，≈7.5MB）

位置：`/Users/banqiu/Downloads/workbuddy 项目/开机静默/`

## 做法（唯一可行路径）
V20 源码已遗失，且任何「从零重写」(AppKit/SwiftUI) 都无法 1:1 还原原版 UI/文案。因此 V21 **直接基于原版 V20 二进制打包**，零改动行为：

1. 从原版 V20 DMG `cp -R` 复制整个 `.app`（UI/图标/功能 100% 保留）。
2. 只改 `Info.plist`：版本号 `20 → 21`，新增 `NSHumanReadableCopyright = "Copyright © 2026 banqiu. Released under the MIT License."`。
3. 原版 V20 **无任何「关于」菜单项**（app 菜单为空），故 plist 版权永不显示。最终用 `about_inject.dylib` 注入：
   - 壳脚本 `SilentLauncher`（sh）设 `DYLD_INSERT_LIBRARIES` 指向 `Resources/about_inject.dylib`，再 `exec SilentLauncher.real`（原版二进制）。
   - dylib 在 `NSApplicationDidFinishLaunchingNotification` 后向 app 菜单插入「关于 开机静默启动器」+ 分隔线 +「退出 开机静默启动器」。
   - About 用 AppleScript `display dialog` 弹窗（NSAlert/NSPanel/系统关于面板在原版内均静默不渲染，仅 AppleScript 能穿透窗口管理真正显示），含 app 名、版本、版权与 MIT 许可说明。
4. `xattr -cr` + `codesign --force --deep --sign -` ad-hoc 重签（无 hardened runtime，DYLD 注入可用）。

## 首次启动权限引导（本版新增）
启动后 dylib 在 `NSApplicationDidFinishLaunchingNotification` 先调用 `ShowFirstLaunchGuide()`，再注入关于菜单：
1. AppleScript `display dialog` 弹说明框，列出三项权限及设置路径（V20 内只有 AppleScript dialog 能渲染）。
2. 调用 `AXIsProcessTrustedWithOptions(prompt=true)` **直接弹出系统「辅助功能」授权窗**。
3. 发一条无害 `tell application "System Events" to get name of first process` **主动触发「自动化」对 System Events 的授权弹窗**（核心隐藏窗口功能依赖）。
4. 写标记 `~/Library/Application Support/com.silentlauncher/.firstlaunch_permissions_v21` → **首次启动只弹一次**（如需重看可删该标记文件）。

### ⚠️ 关键实现约束（中文必须走运行时资源文件）
本机 clang 在 `-mmacosx-version-min=10.15` 下会**静默丢弃中文 `@"..."` CFString 字面量**（最小复现已验证）。因此所有中文 UI 文案放在 `Resources/strings.txt`（UTF-8，key=value），**运行时 `stringWithContentsOfFile:encoding:NSUTF8StringEncoding` 加载**；dylib 源码保持纯 ASCII。已验证：dylib 内无任何中文 UI 串，中文只存在于 strings.txt（关于/欢迎/辅助功能 等字节均不在 dylib）。此约束同时修复了此前「关于」弹窗中文被丢弃的问题。

## 结构核对（已挂载验证）
- `MacOS/SilentLauncher`：shell 包装脚本
- `MacOS/SilentLauncher.real`：原版 Mach-O（universal / x86_64），未改动
- `Resources/about_inject.dylib`：注入用动态库（universal，纯 ASCII 源码，无中文串）
- `Resources/strings.txt`：中文 UI 文案（UTF-8，首次启动引导 + 关于弹窗共用）
- `Resources/AppIcon.icns`：原版图标（2.2MB）
- plist：`CFBundleName=开机静默启动器`、`CFBundleVersion=21`、`NSHumanReadableCopyright=Copyright © 2026 banqiu. Released under the MIT License.`

## 结论
V21 = 原版 V20 行为 100% 一致 + 版本号 21 + 可见的「关于」版权弹窗。两个 DMG 已构建并通过结构校验，可直接拖入「应用程序」使用。

## 待用户验收
- 首次打开 App，应自动弹出「欢迎使用 开机静默启动器」说明框 + 系统「辅助功能」授权窗；点「确定」后按引导去「系统设置→隐私与安全性」授予辅助功能/自动化。
- 点左上角菜单「关于 开机静默启动器」确认弹窗显示中文版权信息（本版已修复中文显示）。
- 授权后**退出并重新打开** App，确认辅助功能生效、隐藏窗口功能可用。
- 确认无误后再决定是否推双远程 / 更新发行附件（此前 v20 已发布，v21 为新增）。
